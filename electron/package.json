{
  "name": "electron-react-boilerplate-electron",
  "version": "1.0.0",
  "description": "Electron + React boilerplate using create-react-app",
  "main": "main.js",
  "scripts": {
    "electron": "electron .",
    "electron:prod": "electron ../dist",
    "start": "cross-env NODE_ENV=dev ELECTRON_START_URL=http://localhost:3000 nodemon --watch . --exec 'npm run electron'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Diego Bonavida <gecko.bona@gmail.com>",
  "devDependencies": {
    "cross-env": "^5.2.0",
    "electron": "^4.0.5"
  },
  "dependencies": {
    "nodemon": "^1.18.10"
  }
}
